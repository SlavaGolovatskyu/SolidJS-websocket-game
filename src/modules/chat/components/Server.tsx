import { Component } from 'solid-js';

type ServerDetailsProps = {
  id: number;
  name: string;
  roomId: number;
};

export const ServerDetails: Component<ServerDetailsProps> = (props) => {
  return <div></div>;
};
